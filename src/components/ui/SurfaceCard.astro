---
interface Props {
  class?: string;
  id?: string;
}

const { class: className = "", id } = Astro.props;
---

<div id={id} class={`bg-surface rounded-2xl border border-subtle shadow-sm p-6 md:p-8 ${className}`}>
  <slot />
</div>
